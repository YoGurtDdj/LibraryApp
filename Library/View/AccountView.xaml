<UserControl x:Class="Library.View.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.View"
             mc:Ignorable="d" d:DesignWidth="800" Height="830">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Visible" Height="830">
        <StackPanel Margin="0,0,0,0" Width="800" Height="830">
            <Image Source="C:\Users\islam\source\repos\RS\Library\Library\View\user.png" Height="270" Width="270"/>
            <Label Name="LoginLabel"  Content="Login" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="24" FontFamily="Inder"/>

            <Button Width="320" Height="50" Content="Выйти из аккаунта" x:Name="Out" Background="#FF393939" BorderThickness="0" Foreground="White" FontSize="24" FontFamily="Inder" Margin="0,20,0,0" Click="Out_Click" Command="{Binding DataContext.AutorizationViewCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="15"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Width="320" Height="50" Content="Удалить аккаунт" x:Name="DeleteAccount" Background="#FFBD0000" BorderThickness="0" Foreground="White" FontSize="24" FontFamily="Inder" Margin="0,20,0,0" Click="DeleteAccount_Click" Command="{Binding DataContext.RegistrationViewCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
                <Button.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="15"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </ScrollViewer>
</UserControl>
